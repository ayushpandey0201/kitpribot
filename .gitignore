# ── Python / env ──────────────────────────────────────────────
venv/
.venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# ── OS ────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
ehthumbs.db
Thumbs.db

# ── Secrets / runtime logs (token appears in httpx URLs) ─────
*.log
nohup.out
.env

# ── Checkpoints: block by default, allow ONLY the two shipped
#    student models (8.5 MB + 2.9 MB, both under GitHub limits).
#    The AST teacher (~329 MiB) must NEVER be committed. ───────
*.pt
*.pth
*.ckpt
!inference/student_mobilenet_fp32.pt
!inference/student_mobilenet_int8_scripted.pt
best_model.pt
*teacher*.pt

# ── Dataset audio — remote (Kaggle), never committed ──────────
*.wav
*.mp3
*.ogg
*.m4a
*.flac
audio_32k/
data/
kitpri_v4_build/

# ── Local artifact archive (incl. 329 MB teacher) ─────────────
samsung_results/

# ── Training run outputs ──────────────────────────────────────
runs/

# ── Internal AI-session working docs (local only) ────────────
docs/IDE_CLAUDE_*.md
